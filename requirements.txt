# Adaptive Hazard Intelligence - Streamlit Cloud Deployment
# Core
streamlit>=1.28.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
scipy>=1.10.0

# Mapping
folium>=0.14.0
streamlit-folium>=0.15.0
geopandas>=0.14.0

# PyTorch (CPU only for Streamlit Cloud)
torch>=2.0.0
torchvision>=0.15.0
--extra-index-url https://download.pytorch.org/whl/cpu

# Data
pyarrow>=14.0.0

# Optional: LLM summarization (comment out if not using)
# llama-cpp-python>=0.2.0
